v input iris
v membagi input menjadi mini-batch
x fungsi loss
  v sumsquarederror
  - crossentropy
x turunan fungsi aktivasi
  v linear, relu, sigmoid
  - softmax
- gradien softmax


- update weight hidden -> rania
- update weight output -> karina
- calculate gradient -> delisha
- build model (.txt) -> kak zaidan
  Parameter yang wajib ada:
  - struktur jaringan (jumlah layer, jumlah neuron setiap layer, fungsi aktivasi setiap layer)
  - learning-rate
  - error threshold
  - max_iter
  v batch_size

- do backpropagation (di model?), kondisi berhenti -> error kumulatif/max_iter




gradient = -1 + act()
v self.type layer (output, hidden)

nanti bareng bareng:
- bandingin pake sklearn 